{
  "name": "rideconnection-program-finder",
  "version": "0.1.0",
  "description": "",
  "main": "dist/index.js",
  "scripts": {
    "test": "NODE_ENV=test jest",
    "build": "preact build --no-prerender --clean --template src/index.ejs --service-worker false",
    "serve": "preact serve",
    "dev": "preact watch --template src/index.ejs"
  },
  "keywords": [],
  "author": "nomeq",
  "license": "User",
  "devDependencies": {
    "eslint": "^4.5.0",
    "jest": "^26.1.0",
    "node-sass": "^4.5.3",
    "preact-cli": "^2.0.2",
    "sass-loader": "^6.0.6"
  },
  "dependencies": {
    "preact": "^8.5.2",
    "preact-compat": "^3.17.0",
    "preact-habitat": "^3.1.0",
    "preact-render-to-string": "^4.1.0"
  },
  "jest": {
    "moduleNameMapper": {
      "^react$": "preact/compat",
      "^react-dom/test-utils$": "preact/test-utils",
      "^react-dom$": "preact/compat"
    }
  }
}
